// 鼠标点击事件处理函数
function handleMouseClick(x, y, z, geoName) {
    // 弹出点击事件传递的参数
    {
        alert('鼠标点击事件触发！\n点击位置: (' + x + ', ' + y + ', ' + z + ')\n几何物体名称: ' + geoName);
    }
    // 在这里可以添加更多的处理逻辑
}