SET(EXE_NAME osgVerse_PreTest_GL)
SET(EXECUTABLE_FILES glfw_test.cpp)
INCLUDE_DIRECTORIES(..)

NEW_EXECUTABLE(${EXE_NAME} SHARED)
TARGET_LINK_LIBRARIES(${EXE_NAME} glfw ${EXTERNAL_LIBRARIES})
TARGET_COMPILE_OPTIONS(${EXE_NAME} PUBLIC -D_SCL_SECURE_NO_WARNINGS)
